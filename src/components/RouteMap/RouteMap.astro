---
import { Image } from 'astro:assets';
const { class: classes, ...rest } = Astro.props;
import routeMap from '@images/track-map.png';
---

<Image
  src={routeMap}
  alt="Route Map"
  width={450}
  densities={[1.5, 2, 3]}
  class={classes}
  {...rest}
/>
